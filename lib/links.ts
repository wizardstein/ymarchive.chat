// External links / contact points used across the app. Centralised so you
// can swap them in one place (e.g. point feedback at a different email, or
// change the Buy Me a Coffee handle).

export const BUY_ME_A_COFFEE_URL = "https://buymeacoffee.com/rainboweng";

// Note: the feedback recipient address is configured via the
// FEEDBACK_TO_EMAIL server environment variable and never ships to the
// client bundle. See app/api/feedback/route.ts.
